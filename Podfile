# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'ISSOverhead' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ISSOverhead
  pod "ASUserDefaults"
  pod "Argo"
  pod "Runes"
  pod "Curry"
  pod "Alamofire", "~> 3.4"
  pod "Notify"

  target 'ISSOverheadTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ISSOverheadUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
